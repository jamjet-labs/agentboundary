name: 07-bad-timestamp-format
description: |
  Scenario forces issued_at to a non-RFC-3339 string.
  Expected: receipt fails schema (format=date-time enforced via jsonschema[format-nongpl]).
setup:
  policies:
    - name: agentboundary.repo.merges
      version: "2"
      rule: allow
      capabilities: [github.merge]
action:
  actor: { type: agent, id: "agent:claude-code:session/demo" }
  agent:
    framework: claude-code
    framework_version: "2.4.1"
    model: claude-opus-4-7
    model_version: "20260101"
  tool: { name: github-mcp, version: "0.7.0", capability: github.merge }
  target:
    system: github.com/jamjet-labs/agentboundary
    environment: prod
    resource_id: pull/14
  arguments: { ref: refs/heads/main, sha: b1c2d3e4 }
  inject:
    - { op: mutate_field, path: issued_at, value: "not a timestamp", after: emit }
expect:
  receipt_must_validate: false
  failures_must_include: ["SCHEMA_INVALID"]
